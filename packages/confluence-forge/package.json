{
  "name": "confluence-forge-wavedrom",
  "version": "0.5.0",
  "private": true,
  "description": "WaveDrom Confluence Cloud (Forge) macro — visual timing-diagram editor.",
  "license": "MIT",
  "scripts": {
    "build:ui": "FORGE_UI_ENTRY=macro vite build && FORGE_UI_ENTRY=editor vite build",
    "build": "npm run build:ui && cd ../.. && npm run build:forge:bundles",
    "deploy": "forge deploy",
    "install:dev": "forge install"
  },
  "dependencies": {
    "@forge/api": "^4.0.0",
    "@forge/bridge": "^4.0.0",
    "@forge/resolver": "^1.6.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
